unit clsDistance_u;
// Calculate the distance between the 2 coordinates around the globe
interface
type
  TDistance = class(TObject)
     private

     public

     end;

implementation
uses SysUtils, math;

end.
